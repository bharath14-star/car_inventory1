{
  "name": "car-portal",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://bharath14-star.github.io/car_portal",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "install:all": "npm install && npm install -w backend && npm install -w frontend",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "animate.css": "^4.1.1"
  }
}
